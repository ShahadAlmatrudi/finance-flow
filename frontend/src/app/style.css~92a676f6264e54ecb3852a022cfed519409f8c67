:root {
    --primary: #2563eb;
    --primary-dark: #1d4ed8;
    --primary-soft: #dbeafe;
    --secondary: #eff6ff;
    --background: #f8fafc;
    --surface: #ffffff;
    --surface-soft: rgba(255, 255, 255, 0.88);
    --text-dark: #0f172a;
    --text-light: #64748b;
    --border: #dbeafe;
    --line: #e2e8f0;
    --shadow: 0 18px 45px rgba(15, 23, 42, 0.08);

    --radius: 20px;
    --radius-sm: 14px;
    --page-side: 8%;
    --section-space: 88px;
}

/* =========================
   RESET + BASE
========================= */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

html {
    scroll-behavior: smooth;
}

body {
    font-family: Arial, Helvetica, sans-serif;
    background: var(--background);
    color: var(--text-dark);
    line-height: 1.6;
}

a {
    text-decoration: none;
}

input,
select,
button,
a {
    transition: 0.2s ease;
}

/* =========================
   REUSABLE BUTTONS / LINKS
========================= */
.primaryBtn,
.secondaryBtn,
.smallBtn,
.smallPrimaryBtn,
.loginbtn,
.headbtn,
.secondaryWideBtn {
    transition: 0.25s ease;
}

.primaryBtn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background: var(--primary);
    color: white;
    border: none;
    padding: 14px 24px;
    border-radius: 14px;
    font-weight: 700;
    min-width: 130px;
    cursor: pointer;
}

.primaryBtn:hover {
    background: var(--primary-dark);
    transform: translateY(-1px);
}

.secondaryBtn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background: white;
    color: var(--primary);
    border: 1px solid var(--primary);
    padding: 14px 24px;
    border-radius: 14px;
    font-weight: 700;
    min-width: 130px;
    cursor: pointer;
}

.secondaryBtn:hover {
    background: var(--secondary);
}

.secondaryWideBtn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background: #e8edff;
    color: var(--text-dark);
    border: none;
    padding: 14px 24px;
    border-radius: 14px;
    font-weight: 700;
    width: 100%;
    cursor: pointer;
}

.secondaryWideBtn:hover {
    background: #dbe4ff;
}

.smallBtn {
    background: #edf2f7;
    color: var(--text-dark);
    border: none;
    padding: 10px 16px;
    border-radius: 12px;
    font-weight: 600;
    cursor: pointer;
    width: fit-content;
}

.smallBtn:hover {
    background: #e2e8f0;
}

.smallPrimaryBtn {
    background: var(--primary);
    color: white;
    border: none;
    padding: 10px 18px;
    border-radius: 12px;
    font-weight: 700;
    cursor: pointer;
    width: fit-content;
}

.smallPrimaryBtn:hover {
    background: var(--primary-dark);
}

.fullBtn {
    width: 100%;
}

.textLink {
    color: var(--primary);
    font-weight: 600;
}

.textLink:hover {
    text-decoration: underline;
}

/* =========================
   GUEST PAGE
========================= */
.header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 18px var(--page-side);
    background: rgba(255, 255, 255, 0.85);
    border-bottom: 1px solid var(--line);
    position: sticky;
    top: 0;
    z-index: 1000;
    backdrop-filter: blur(12px);
}

.logo {
    font-size: 1.4rem;
    font-weight: 800;
    color: var(--primary);
}

.headerbtns {
    display: flex;
    align-items: center;
    gap: 16px;
}

.headbtn {
    color: var(--text-dark);
    padding: 10px 14px;
    border-radius: 10px;
    font-weight: 600;
}

.headbtn:hover {
    background: var(--secondary);
}

.login {
    display: flex;
    gap: 10px;
    margin-left: 10px;
}

.loginbtn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 10px 18px;
    border-radius: 12px;
    font-weight: 700;
}

.login-outline {
    background: white;
    color: var(--primary);
    border: 1px solid var(--primary);
}

.login-outline:hover {
    background: var(--secondary);
}

.login-fill {
    background: var(--primary);
    color: white;
}

.login-fill:hover {
    background: var(--primary-dark);
}

.aboutUs {
    padding: 100px var(--page-side);
    background: linear-gradient(135deg, #eff6ff 0%, #dbeafe 55%, #f8fbff 100%);
}

.heroContent {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 40px;
    flex-wrap: wrap;
}

.heroText {
    flex: 1 1 520px;
    max-width: 650px;
}

.heroBadge {
    display: inline-block;
    background: white;
    color: var(--primary);
    border: 1px solid var(--border);
    padding: 8px 14px;
    border-radius: 999px;
    font-size: 0.9rem;
    font-weight: 700;
    margin-bottom: 18px;
}

.aboutUs h1 {
    font-size: 3rem;
    line-height: 1.15;
    margin-bottom: 20px;
    color: var(--text-dark);
}

.aboutUs p {
    max-width: 600px;
    color: var(--text-light);
    font-size: 1.08rem;
    margin-bottom: 30px;
}

.heroActions {
    display: flex;
    gap: 14px;
    flex-wrap: wrap;
}

.heroPreview {
    flex: 1 1 280px;
    display: grid;
    gap: 18px;
    max-width: 330px;
}

.previewCard {
    background: rgba(255, 255, 255, 0.85);
    border: 1px solid rgba(255, 255, 255, 0.7);
    box-shadow: var(--shadow);
    border-radius: var(--radius);
    padding: 24px;
    backdrop-filter: blur(10px);
}

.miniTitle {
    color: var(--text-light);
    font-size: 0.92rem;
}

.previewCard h3 {
    font-size: 2rem;
    margin: 8px 0 6px;
}

.miniMuted {
    color: var(--text-light);
}

.featuresSection {
    padding: var(--section-space) var(--page-side);
    text-align: center;
}

.featuresSection h2 {
    font-size: 2.2rem;
    margin-bottom: 10px;
}

.sectionIntro {
    max-width: 650px;
    margin: 0 auto 40px;
    color: var(--text-light);
}

.features {
    display: flex;
    justify-content: center;
    gap: 24px;
    flex-wrap: wrap;
}

.feature {
    background: var(--surface);
    width: 320px;
    padding: 28px 24px;
    border-radius: var(--radius);
    box-shadow: var(--shadow);
    border: 1px solid var(--line);
    text-align: left;
    transition: 0.25s ease;
}

.feature:hover {
    transform: translateY(-6px);
}

.featureIcon {
    font-size: 1.6rem;
    margin-bottom: 14px;
}

.feature h3 {
    color: var(--text-dark);
    margin-bottom: 10px;
    font-size: 1.2rem;
}

.feature p {
    color: var(--text-light);
}

.contacts {
    padding: 60px var(--page-side);
    text-align: center;
    background: var(--text-dark);
    color: white;
}

.contacts h2 {
    margin-bottom: 12px;
}

.contacts p {
    color: #cbd5e1;
}

/* =========================
   AUTH + FLOW SHARED
========================= */
.authPage,
.flowPage {
    min-height: 100vh;
    background: linear-gradient(135deg, #eff6ff 0%, #dbeafe 45%, #f8fafc 100%);
}

.simpleTopbar {
    padding: 24px var(--page-side);
}

.centerPage,
.flowWrapper {
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 20px;
}

.centerPage {
    min-height: calc(100vh - 80px);
}

.flowWrapper {
    min-height: 100vh;
    padding: 40px 20px;
}

.formCard,
.wideCard {
    background: var(--surface-soft);
    backdrop-filter: blur(14px);
    border: 1px solid rgba(255,255,255,0.7);
    box-shadow: var(--shadow);
    border-radius: var(--radius);
}

.formCard {
    width: 100%;
    max-width: 500px;
    padding: 38px 32px;
}

.largeCard {
    max-width: 700px;
}

.extraLargeCard {
    max-width: 860px;
}

.wideCard {
    width: 100%;
    max-width: 960px;
    padding: 40px 34px;
}

.pageTitle {
    text-align: center;
    font-size: 2rem;
    margin-bottom: 10px;
}

.pageSubtitle {
    text-align: center;
    color: var(--text-light);
    margin-bottom: 28px;
}

.stepHeader {
    margin-bottom: 24px;
}

.centerHeader {
    text-align: center;
}

.stepTag {
    display: inline-block;
    background: white;
    color: var(--primary);
    border: 1px solid var(--border);
    padding: 7px 12px;
    border-radius: 999px;
    font-size: 0.85rem;
    font-weight: 700;
    margin-bottom: 14px;
}

.centerHeader .stepTag {
    margin-left: auto;
    margin-right: auto;
    display: inline-block;
}

.mainForm {
    display: flex;
    flex-direction: column;
    gap: 18px;
}

.inputGroup {
    display: flex;
    flex-direction: column;
    gap: 8px;
}

.inputGroup label {
    font-weight: 700;
    font-size: 0.95rem;
    color: var(--text-dark);
}

.inputGroup input,
.inputGroup select,
.cashRow input {
    width: 100%;
    padding: 14px 16px;
    border: 1px solid #cbd5e1;
    border-radius: 14px;
    background: white;
    font-size: 1rem;
    outline: none;
    color: var(--text-dark);
}

.inputGroup input:focus,
.inputGroup select:focus,
.cashRow input:focus {
    border-color: var(--primary);
    box-shadow: 0 0 0 4px rgba(37, 99, 235, 0.12);
}

.rowBetween {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 12px;
}

.checkLabel,
.optionLabel {
    color: var(--text-dark);
    display: flex;
    align-items: center;
    gap: 10px;
}

.optionLabel {
    padding: 8px 10px;
    border-radius: 12px;
    cursor: pointer;
}

.optionLabel:hover {
    background: var(--secondary);
}

.optionLabel input {
    accent-color: var(--primary);
}

.bottomText {
    margin-top: 24px;
    text-align: center;
    color: var(--text-light);
}

.actionRow {
    margin-top: 20px;
    display: flex;
    justify-content: center;
    gap: 14px;
    flex-wrap: wrap;
}

.dualButtons {
    justify-content: center;
}

.formRow {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 16px;
}

.cardSection {
    background: rgba(255,255,255,0.8);
    border: 1px solid rgba(255,255,255,0.7);
    box-shadow: 0 10px 25px rgba(15,23,42,0.04);
    border-radius: 18px;
    padding: 24px;
    margin-bottom: 24px;
}

.subSectionTitle {
    font-size: 1.35rem;
    margin-bottom: 18px;
}

/* =========================
   QUESTIONNAIRE
========================= */
.questionnaireCard {
    max-width: 1020px;
}

.questionnaireForm {
    margin-top: 10px;
}

.twoColumnForm {
    display: grid;
    grid-template-columns: repeat(2, minmax(260px, 1fr));
    gap: 28px 34px;
}

.questionBlock {
    display: flex;
    flex-direction: column;
    gap: 12px;
}

.questionBlock h3 {
    font-size: 1.08rem;
    margin-bottom: 10px;
    color: var(--text-dark);
}

.questionCard {
    background: rgba(255, 255, 255, 0.72);
    border: 1px solid rgba(255, 255, 255, 0.8);
    border-radius: 18px;
    padding: 22px 20px;
    box-shadow: 0 8px 22px rgba(15, 23, 42, 0.04);
    transition: 0.25s ease;
}

.questionCard:hover {
    transform: translateY(-2px);
    box-shadow: 0 12px 28px rgba(15, 23, 42, 0.08);
}

.fullWidth {
    grid-column: 1 / -1;
}

.checkboxGrid {
    display: grid;
    grid-template-columns: repeat(2, minmax(180px, 1fr));
    gap: 12px 18px;
    margin-top: 4px;
}

.questionnaireActions {
    margin-top: 12px;
}

/* =========================
   PROFILE
========================= */
.profileCard {
    max-width: 760px;
}

.profileForm {
    margin-top: 10px;
}

.profileCard .inputGroup {
    background: rgba(255, 255, 255, 0.55);
    border: 1px solid rgba(255, 255, 255, 0.7);
    border-radius: 18px;
    padding: 16px 16px 12px;
    box-shadow: 0 8px 20px rgba(15, 23, 42, 0.03);
}

.profileCard .inputGroup input,
.profileCard .inputGroup select {
    background: white;
}

.profileCard .actionRow {
    margin-top: 8px;
}

/* =========================
   PLAN SETUP
========================= */
.planCard {
    max-width: 920px;
}

.planCard .cardSection {
    background: rgba(255, 255, 255, 0.72);
    border: 1px solid rgba(255,255,255,0.8);
    box-shadow: 0 10px 25px rgba(15,23,42,0.04);
}

.planCard .inputGroup input,
.planCard .inputGroup select {
    background: white;
}

.sectionHelper {
    color: var(--text-light);
    margin-bottom: 18px;
}

.checkboxOption {
    margin-top: 10px;
}

.categoryGrid {
    display: grid;
    grid-template-columns: repeat(2, minmax(220px, 1fr));
    gap: 14px;
    margin: 18px 0 20px;
}

.categoryChoice {
    display: flex;
    align-items: center;
    gap: 10px;
    background: rgba(255, 255, 255, 0.65);
    border: 1px solid var(--line);
    border-radius: 14px;
    padding: 14px 16px;
    cursor: pointer;
    transition: 0.2s ease;
}

.categoryChoice:hover {
    background: var(--secondary);
}

.categoryChoice input {
    accent-color: var(--primary);
}

.budgetInputs {
    display: grid;
    grid-template-columns: repeat(2, minmax(240px, 1fr));
    gap: 16px;
}

.budgetInput input:disabled {
    background: #f1f5f9;
    color: #94a3b8;
    cursor: not-allowed;
}

/* =========================
   CARDS PAGE
========================= */
.cardsPageCard {
    max-width: 940px;
}

.cardsPreview {
    display: flex;
    gap: 20px;
    flex-wrap: wrap;
    margin-bottom: 24px;
}

.emptyState {
    width: 100%;
    text-align: center;
    padding: 28px 20px;
    border: 1px dashed #cbd5e1;
    border-radius: 16px;
    color: var(--text-light);
    background: rgba(255, 255, 255, 0.55);
}

.savedCard {
    flex: 1 1 280px;
    min-height: 180px;
    border-radius: 22px;
    color: white;
    padding: 24px;
    box-shadow: var(--shadow);
    position: relative;
    overflow: hidden;
}

.savedCard::after {
    content: "";
    position: absolute;
    inset: 0;
    background: linear-gradient(to bottom right, rgba(255,255,255,0.12), rgba(255,255,255,0));
    pointer-events: none;
}

.savedVisa {
    background: linear-gradient(135deg, #3b82f6, #2563eb);
}

.savedMastercard {
    background: linear-gradient(135deg, #a855f7, #7c3aed);
}
.savedMada {
    background: linear-gradient(135deg, #22c55e, #15803d);
}

.savedCardTop {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 26px;
    position: relative;
    z-index: 1;
}

.savedCardBrand {
    font-size: 1.15rem;
    font-weight: 800;
}

.primaryBadge {
    background: rgba(255, 255, 255, 0.18);
    border: 1px solid rgba(255,255,255,0.35);
    padding: 5px 10px;
    border-radius: 999px;
    font-size: 0.75rem;
    font-weight: 700;
}

.savedCardNumber {
    font-size: 1.3rem;
    letter-spacing: 2px;
    margin-bottom: 22px;
    position: relative;
    z-index: 1;
}

.savedCardBottom {
    position: relative;
    z-index: 1;
}

.savedCardName,
.savedCardExpiry {
    font-size: 1rem;
    opacity: 0.96;
}

.inlineCheckGroup {
    justify-content: flex-end;
    display: flex;
    align-items: end;
}

.leftActions {
    justify-content: flex-start;
}

.cashRow {
    display: grid;
    grid-template-columns: 1fr auto;
    gap: 14px;
}

.cashSaveBtnWrap {
    display: flex;
    align-items: end;
}

.cashDisplay {
    margin-top: 14px;
    background: rgba(255,255,255,0.65);
    border: 1px solid var(--line);
    border-radius: 14px;
    padding: 14px 16px;
    color: var(--text-dark);
    font-weight: 600;
}

/* =========================
   WELCOME
========================= */
.welcomeCard {
    max-width: 780px;
    text-align: center;
    padding: 54px 36px;
}

.welcomeEmoji {
    font-size: 2.6rem;
    margin-bottom: 10px;
}

.welcomeActions {
    display: flex;
    flex-direction: column;
    gap: 16px;
    margin: 28px 0 22px;
}

/* =========================
   ERROR STATES
========================= */
.errorMsg {
    color: #dc2626;
    font-size: 0.85rem;
    margin-top: 4px;
    display: block;
    min-height: 18px;
}

.inputError {
    border: 2px solid #dc2626 !important;
    background-color: #fef2f2 !important;
}

/* =========================
   RESPONSIVE
========================= */
@media (max-width: 900px) {
    .header {
        flex-direction: column;
        gap: 14px;
    }

    .headerbtns {
        flex-wrap: wrap;
        justify-content: center;
    }

    .heroContent {
        flex-direction: column;
        align-items: flex-start;
    }

    .aboutUs h1 {
        font-size: 2.4rem;
    }

    .twoColumnForm {
        grid-template-columns: 1fr;
    }

    .checkboxGrid {
        grid-template-columns: 1fr;
    }
}

@media (max-width: 700px) {
    .formRow,
    .cashRow,
    .categoryGrid,
    .budgetInputs {
        grid-template-columns: 1fr;
    }

    .wideCard,
    .formCard {
        padding: 28px 20px;
    }

    .pageTitle {
        font-size: 1.7rem;
    }

    .aboutUs {
        padding: 76px 6%;
    }

    .featuresSection,
    .contacts,
    .simpleTopbar,
    .header {
        padding-left: 6%;
        padding-right: 6%;
    }

    .cashSaveBtnWrap {
        align-items: stretch;
    }
}

@media (max-width: 520px) {
    .login {
        flex-direction: column;
        width: 100%;
        margin-left: 0;
    }

    .loginbtn,
    .headbtn {
        width: 100%;
        justify-content: center;
        text-align: center;
    }

    .headerbtns {
        width: 100%;
        flex-direction: column;
    }

    .rowBetween {
        flex-direction: column;
        align-items: flex-start;
    }

    .aboutUs h1 {
        font-size: 2rem;
    }

    .heroActions {
        width: 100%;
    }

    .heroActions a {
        width: 100%;
        justify-content: center;
    }
}

.welcomePageBody {
    min-height: 100vh;
    margin: 0;
    background: linear-gradient(180deg, #f8fbff 0%, #eef4ff 100%);
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 24px;
}

.welcomePage {
    width: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
}

.welcomeCard {
    width: 100%;
    max-width: 720px;
    background: #ffffff;
    border: 1px solid #e5eefc;
    border-radius: 24px;
    padding: 40px 32px;
    box-shadow: 0 20px 50px rgba(15, 23, 42, 0.08);
    text-align: center;
}

.welcomeIconWrap {
    width: 82px;
    height: 82px;
    margin: 0 auto 18px;
    border-radius: 50%;
    background: #eef4ff;
    display: flex;
    align-items: center;
    justify-content: center;
}

.welcomeEmoji {
    font-size: 34px;
}

.welcomeTitle {
    margin: 0;
    font-size: 2rem;
    font-weight: 800;
    color: #0f172a;
    letter-spacing: -0.02em;
}

.welcomeSubtitle {
    margin: 14px auto 0;
    max-width: 520px;
    color: #64748b;
    font-size: 1rem;
    line-height: 1.7;
}

.welcomeSummary {
    margin-top: 30px;
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 16px;
}

.welcomeSummaryItem {
    background: #f8fbff;
    border: 1px solid #e2e8f0;
    border-radius: 18px;
    padding: 18px 16px;
    text-align: left;
}

.summaryLabel {
    display: block;
    font-size: 0.9rem;
    color: #64748b;
    margin-bottom: 8px;
}

.summaryValue {
    display: block;
    font-size: 1.1rem;
    font-weight: 700;
    color: #0f172a;
    word-break: break-word;
}

.welcomeActions {
    margin-top: 28px;
    display: flex;
    flex-direction: column;
    gap: 14px;
}

.welcomeMainBtn,
.welcomeSecondaryBtn {
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 54px;
    text-decoration: none;
}

.welcomeSecondaryBtn {
    cursor: pointer;
}

.welcomeHelpText {
    margin-top: 20px;
    font-size: 0.95rem;
    color: #64748b;
}

@media (max-width: 640px) {
    .welcomeCard {
        padding: 28px 20px;
        border-radius: 20px;
    }

    .welcomeTitle {
        font-size: 1.6rem;
    }

    .welcomeSummary {
        grid-template-columns: 1fr;
    }
}

.appPageBody {
    margin: 0;
    min-height: 100vh;
    background: #f8fafc;
    color: #0f172a;
}

.appLayout {
    display: flex;
    min-height: 100vh;
}

.sidebar {
    width: 270px;
    background: #ffffff;
    border-right: 1px solid #e2e8f0;
    padding: 28px 20px;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    box-sizing: border-box;
}

.sidebarBrand {
    margin-bottom: 28px;
}

.sidebarLogo {
    text-decoration: none;
    font-size: 1.55rem;
    font-weight: 800;
    color: #2563eb;
}

.sidebarNav {
    display: flex;
    flex-direction: column;
    gap: 10px;
    flex: 1;
}

.navItem {
    text-decoration: none;
    color: #334155;
    padding: 12px 14px;
    border-radius: 12px;
    font-weight: 600;
    transition: 0.2s ease;
}

.navItem:hover {
    background: #eff6ff;
    color: #2563eb;
}

.navItem.active {
    background: #dbeafe;
    color: #1d4ed8;
}

.sidebarUser {
    display: flex;
    align-items: center;
    gap: 12px;
    padding-top: 20px;
    border-top: 1px solid #e2e8f0;
    margin-top: 24px;
}

.sidebarAvatar {
    width: 46px;
    height: 46px;
    border-radius: 50%;
    background: #dbeafe;
    color: #1d4ed8;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 800;
}

.sidebarUserName {
    margin: 0;
    font-weight: 700;
    color: #0f172a;
}

.sidebarLogoutBtn {
    margin-top: 4px;
    border: none;
    background: transparent;
    color: #ef4444;
    padding: 0;
    cursor: pointer;
    font-size: 0.95rem;
}

.mainContent {
    flex: 1;
    padding: 32px;
    box-sizing: border-box;
}

.topBar {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: 18px;
    margin-bottom: 28px;
}

.pageHeading {
    margin: 0;
    font-size: 2rem;
    font-weight: 800;
    color: #0f172a;
}

.pageSubheading {
    margin: 8px 0 0;
    color: #64748b;
    font-size: 1rem;
}

.topBarRight {
    display: flex;
    align-items: center;
    gap: 12px;
}

.searchInput {
    min-width: 240px;
    padding: 12px 14px;
    border-radius: 12px;
    border: 1px solid #dbeafe;
    outline: none;
    background: #ffffff;
}

.notificationBtn {
    width: 46px;
    height: 46px;
    border-radius: 12px;
    background: #ffffff;
    border: 1px solid #dbeafe;
    display: flex;
    align-items: center;
    justify-content: center;
    text-decoration: none;
    font-size: 1.2rem;
}

.financeCards {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 18px;
    margin-bottom: 22px;
}

.financeCard {
    background: #ffffff;
    border-radius: 18px;
    border: 1px solid #e2e8f0;
    padding: 22px;
    box-shadow: 0 10px 24px rgba(15, 23, 42, 0.04);
}

.financeCardTop {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 14px;
}

.financeCardTop h3 {
    margin: 0;
    font-size: 1.15rem;
}

.financeIcon {
    font-size: 1.4rem;
}

.financeLabel {
    margin: 0;
    color: #64748b;
    font-size: 0.95rem;
}

.financeAmount {
    margin: 12px 0 8px;
    font-size: 2rem;
    font-weight: 800;
    color: #0f172a;
}

.financeSmallText {
    margin: 0 0 16px;
    color: #64748b;
    min-height: 22px;
}

.cardActionBtn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    text-decoration: none;
    background: #3b82f6;
    color: white;
    padding: 10px 14px;
    border-radius: 10px;
    font-weight: 600;
}

.dashboardGrid {
    display: grid;
    grid-template-columns: 2fr 1fr;
    gap: 18px;
}

.dashboardPanel {
    background: #ffffff;
    border-radius: 18px;
    border: 1px solid #e2e8f0;
    padding: 22px;
    box-shadow: 0 10px 24px rgba(15, 23, 42, 0.04);
}

.panelHeader {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 16px;
}

.panelHeader h2 {
    margin: 0;
    font-size: 1.2rem;
}

.panelLink {
    text-decoration: none;
    color: #2563eb;
    font-weight: 600;
}

.transactionList {
    display: flex;
    flex-direction: column;
    gap: 12px;
}

.transactionItem {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
    padding: 14px 0;
    border-bottom: 1px solid #eef2f7;
}

.transactionItem:last-child {
    border-bottom: none;
}

.transactionLeft h4 {
    margin: 0 0 6px;
    font-size: 1rem;
}

.transactionLeft p {
    margin: 0;
    color: #64748b;
    font-size: 0.92rem;
}

.transactionRight {
    font-weight: 800;
    font-size: 1rem;
}

.positiveAmount {
    color: #16a34a;
}

.negativeAmount {
    color: #ef4444;
}

.progressList {
    display: flex;
    flex-direction: column;
    gap: 18px;
}

.progressItem {
    display: flex;
    flex-direction: column;
    gap: 8px;
}

.progressLabelRow {
    display: flex;
    justify-content: space-between;
    gap: 12px;
    font-size: 0.95rem;
    color: #334155;
    font-weight: 600;
}

.progressTrack {
    width: 100%;
    height: 12px;
    background: #e2e8f0;
    border-radius: 999px;
    overflow: hidden;
}

.progressFill {
    height: 100%;
    border-radius: 999px;
}

.progressBlue {
    background: #3b82f6;
}

.progressGreen {
    background: #22c55e;
}

.progressPurple {
    background: #8b5cf6;
}

.progressOrange {
    background: #f59e0b;
}

.categoryList {
    display: flex;
    flex-direction: column;
    gap: 16px;
}

.categoryItem {
    padding-bottom: 14px;
    border-bottom: 1px solid #eef2f7;
}

.categoryItem:last-child {
    border-bottom: none;
    padding-bottom: 0;
}

.categoryTopRow,
.categoryBottomRow {
    display: flex;
    justify-content: space-between;
    gap: 12px;
    margin-bottom: 8px;
}

.categoryName {
    font-weight: 700;
    color: #0f172a;
}

.categoryAmounts,
.categoryBottomRow {
    color: #64748b;
    font-size: 0.92rem;
}

.categoryBar {
    width: 100%;
    height: 12px;
    border-radius: 999px;
    background: #e2e8f0;
    overflow: hidden;
    margin-bottom: 8px;
}

.categoryBarFill {
    height: 100%;
    background: #3b82f6;
    border-radius: 999px;
}

.goalBox {
    display: flex;
    flex-direction: column;
    gap: 10px;
}

.goalType {
    margin: 0;
    color: #2563eb;
    font-weight: 700;
}

.goalName {
    margin: 0;
    font-size: 1.35rem;
    color: #0f172a;
}

.goalMeta {
    margin: 0;
    color: #64748b;
}

.emptyPanelState {
    color: #64748b;
    padding: 10px 0;
    line-height: 1.6;
}

@media (max-width: 1100px) {
    .financeCards {
        grid-template-columns: 1fr;
    }

    .dashboardGrid {
        grid-template-columns: 1fr;
    }
}

@media (max-width: 860px) {
    .appLayout {
        flex-direction: column;
    }

    .sidebar {
        width: 100%;
        border-right: none;
        border-bottom: 1px solid #e2e8f0;
    }

    .mainContent {
        padding: 22px;
    }

    .topBar {
        flex-direction: column;
        align-items: stretch;
    }

    .topBarRight {
        width: 100%;
    }

    .searchInput {
        flex: 1;
        min-width: 0;
        width: 100%;
    }
}

@media (max-width: 560px) {
    .mainContent {
        padding: 16px;
    }

    .pageHeading {
        font-size: 1.6rem;
    }

    .financeAmount {
        font-size: 1.6rem;
    }

    .sidebar {
        padding: 20px 16px;
    }
}

.notificationsTopBar {
    margin-bottom: 20px;
}

.notificationsActions {
    display: flex;
    align-items: center;
    gap: 12px;
}

.notificationsStats {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 16px;
    margin-bottom: 20px;
}

.miniStatCard {
    background: #ffffff;
    border: 1px solid #e2e8f0;
    border-radius: 16px;
    padding: 18px;
    box-shadow: 0 10px 24px rgba(15, 23, 42, 0.04);
}

.miniStatLabel {
    display: block;
    color: #64748b;
    font-size: 0.9rem;
    margin-bottom: 8px;
}

.miniStatValue {
    font-size: 1.7rem;
    color: #0f172a;
    font-weight: 800;
}

.notificationsPanel {
    min-height: 420px;
}

.notificationsList {
    display: flex;
    flex-direction: column;
    gap: 14px;
}

.notificationItem {
    display: flex;
    justify-content: space-between;
    gap: 18px;
    padding: 18px;
    border-radius: 16px;
    border: 1px solid #e2e8f0;
    background: #ffffff;
    transition: 0.2s ease;
}

.notificationItem.unread {
    border-color: #bfdbfe;
    background: #f8fbff;
}

.notificationItem.read {
    opacity: 0.92;
}

.notificationLeft {
    display: flex;
    gap: 14px;
    flex: 1;
}

.notificationContent h3 {
    margin: 0 0 8px;
    font-size: 1.05rem;
    color: #0f172a;
}

.notificationContent p {
    margin: 0;
    color: #64748b;
    line-height: 1.6;
}

.notificationRight {
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    gap: 10px;
    min-width: 120px;
}

.notificationStatus {
    font-size: 0.9rem;
    font-weight: 700;
}

.readStatus {
    color: #64748b;
}

.unreadStatus {
    color: #2563eb;
}

.notificationActionBtn {
    border: none;
    background: #eff6ff;
    color: #1d4ed8;
    padding: 9px 12px;
    border-radius: 10px;
    font-weight: 600;
    cursor: pointer;
}

.notificationTypeBadge {
    min-width: 88px;
    height: fit-content;
    padding: 8px 10px;
    border-radius: 999px;
    font-size: 0.82rem;
    font-weight: 700;
    text-transform: capitalize;
    text-align: center;
}

.badgeGoal {
    background: #ede9fe;
    color: #7c3aed;
}

.badgeBudget {
    background: #fef3c7;
    color: #b45309;
}

.badgeAccount {
    background: #dcfce7;
    color: #15803d;
}

.badgeReminder {
    background: #fee2e2;
    color: #dc2626;
}

.badgeInfo {
    background: #dbeafe;
    color: #1d4ed8;
}

@media (max-width: 1100px) {
    .notificationsStats {
        grid-template-columns: repeat(2, minmax(0, 1fr));
    }
}

@media (max-width: 760px) {
    .notificationItem {
        flex-direction: column;
    }

    .notificationRight {
        align-items: flex-start;
        min-width: 0;
    }
}

@media (max-width: 560px) {
    .notificationsStats {
        grid-template-columns: 1fr;
    }

    .notificationLeft {
        flex-direction: column;
    }

    .notificationTypeBadge {
        min-width: 0;
        width: fit-content;
    }
}

.budgetTopGrid,
.budgetBottomGrid {
    display: grid;
    grid-template-columns: 1.15fr 1fr;
    gap: 18px;
    margin-bottom: 18px;
}

.budgetStatusList {
    display: flex;
    flex-direction: column;
    gap: 18px;
}

.statusRow {
    display: flex;
    flex-direction: column;
    gap: 8px;
}

.statusLabelRow {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    font-weight: 600;
    color: #334155;
}

.categoryManagerList {
    display: flex;
    flex-direction: column;
    gap: 16px;
    margin-bottom: 18px;
}

.categoryManagerItem {
    padding-bottom: 16px;
    border-bottom: 1px solid #eef2f7;
}

.categoryManagerItem:last-child {
    border-bottom: none;
    padding-bottom: 0;
}

.categoryManagerTop {
    display: flex;
    justify-content: space-between;
    gap: 14px;
    margin-bottom: 10px;
}

.categoryManagerTop h3 {
    margin: 0 0 6px;
    font-size: 1rem;
    color: #0f172a;
}

.categoryManagerTop p {
    margin: 0;
    color: #64748b;
    font-size: 0.92rem;
}

.categoryManagerActions {
    display: flex;
    gap: 8px;
    align-items: flex-start;
}

.categoryManagerBottom {
    display: flex;
    justify-content: space-between;
    gap: 12px;
    margin-top: 8px;
    color: #64748b;
    font-size: 0.92rem;
}

.addCategoryForm {
    margin-top: 10px;
    padding-top: 16px;
    border-top: 1px solid #eef2f7;
}

.addCategoryGrid {
    display: grid;
    grid-template-columns: 1fr 1fr auto;
    gap: 10px;
}

.addCategoryGrid input {
    padding: 12px 14px;
    border: 1px solid #dbeafe;
    border-radius: 12px;
    outline: none;
    background: #fff;
}

.accountBalanceList {
    display: flex;
    flex-direction: column;
    gap: 14px;
    margin-bottom: 16px;
}

.accountRow {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 14px;
    padding: 14px 0;
    border-bottom: 1px solid #eef2f7;
}

.accountRow:last-child {
    border-bottom: none;
}

.accountLeft {
    display: flex;
    align-items: center;
    gap: 12px;
}

.accountIcon {
    width: 42px;
    height: 42px;
    border-radius: 12px;
    background: #eff6ff;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.2rem;
}

.accountLeft h4 {
    margin: 0 0 4px;
    color: #0f172a;
}

.accountLeft p {
    margin: 0;
    color: #64748b;
    font-size: 0.92rem;
}

.accountAmount {
    font-weight: 800;
    color: #0f172a;
}

.cashUpdateForm {
    padding-top: 14px;
    border-top: 1px solid #eef2f7;
}

.cashUpdateRow {
    display: grid;
    grid-template-columns: 1fr auto;
    gap: 10px;
}

.cashUpdateRow input {
    padding: 12px 14px;
    border: 1px solid #dbeafe;
    border-radius: 12px;
    outline: none;
    background: #fff;
}

.budgetGoalActions {
    margin-top: 16px;
}

.smallBtn {
    padding: 10px 14px;
    min-height: auto;
    font-size: 0.92rem;
}

.dangerGhostBtn {
    border: 1px solid #fecaca;
    background: #fff5f5;
    color: #dc2626;
    border-radius: 10px;
    padding: 10px 14px;
    cursor: pointer;
    font-weight: 600;
}

@media (max-width: 1100px) {
    .budgetTopGrid,
    .budgetBottomGrid {
        grid-template-columns: 1fr;
    }
}

@media (max-width: 700px) {
    .addCategoryGrid,
    .cashUpdateRow {
        grid-template-columns: 1fr;
    }

    .categoryManagerTop,
    .accountRow {
        flex-direction: column;
        align-items: flex-start;
    }

    .categoryManagerActions {
        width: 100%;
        flex-wrap: wrap;
    }
}

.analyticsTopCards {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 16px;
    margin-bottom: 20px;
}

.analyticsStatCard {
    background: #ffffff;
    border: 1px solid #e2e8f0;
    border-radius: 18px;
    padding: 20px;
    box-shadow: 0 10px 24px rgba(15, 23, 42, 0.04);
}

.analyticsStatLabel {
    display: block;
    color: #64748b;
    font-size: 0.9rem;
    margin-bottom: 10px;
}

.analyticsStatValue {
    margin: 0;
    font-size: 1.8rem;
    font-weight: 800;
    color: #0f172a;
}

.analyticsStatNote {
    margin: 10px 0 0;
    color: #64748b;
    font-size: 0.92rem;
    line-height: 1.5;
}

.analyticsGrid {
    display: grid;
    grid-template-columns: 1.25fr 0.95fr;
    gap: 18px;
}

.analyticsPanelLarge,
.analyticsPanelSmall {
    min-height: 100%;
}

.analyticsCategoryList,
.analyticsInsightsList,
.analyticsPerformanceList,
.analyticsSnapshotList {
    display: flex;
    flex-direction: column;
    gap: 14px;
}

.analyticsCategoryItem {
    padding-bottom: 16px;
    border-bottom: 1px solid #eef2f7;
}

.analyticsCategoryItem:last-child {
    border-bottom: none;
    padding-bottom: 0;
}

.analyticsCategoryTop {
    display: flex;
    justify-content: space-between;
    gap: 12px;
    margin-bottom: 10px;
}

.analyticsCategoryTop h3 {
    margin: 0 0 6px;
    color: #0f172a;
    font-size: 1rem;
}

.analyticsCategoryTop p {
    margin: 0;
    color: #64748b;
    font-size: 0.92rem;
}

.analyticsCategoryPercent {
    font-weight: 800;
    color: #2563eb;
    font-size: 1rem;
}

.analyticsBar {
    width: 100%;
    height: 12px;
    border-radius: 999px;
    background: #e2e8f0;
    overflow: hidden;
    margin-bottom: 8px;
}

.analyticsBarFill {
    height: 100%;
    background: #3b82f6;
    border-radius: 999px;
}

.analyticsCategoryBottom {
    display: flex;
    justify-content: space-between;
    gap: 12px;
    color: #64748b;
    font-size: 0.92rem;
}

.analyticsInsightItem {
    padding: 16px;
    border: 1px solid #e2e8f0;
    border-radius: 14px;
    background: #f8fbff;
}

.analyticsInsightItem h3 {
    margin: 0 0 8px;
    font-size: 1rem;
    color: #0f172a;
}

.analyticsInsightItem p {
    margin: 0;
    color: #64748b;
    line-height: 1.6;
}

.analyticsPerformanceItem {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    padding: 14px 0;
    border-bottom: 1px solid #eef2f7;
}

.analyticsPerformanceItem:last-child {
    border-bottom: none;
}

.analyticsPerformanceLeft h3 {
    margin: 0 0 6px;
    font-size: 1rem;
    color: #0f172a;
}

.analyticsPerformanceLeft p {
    margin: 0;
    color: #64748b;
    font-size: 0.92rem;
}

.analyticsPerformanceRight {
    min-width: 74px;
    text-align: center;
    padding: 10px 12px;
    border-radius: 999px;
    font-weight: 800;
    font-size: 0.95rem;
}

.performanceHealthy {
    background: #dcfce7;
    color: #15803d;
}

.performanceModerate {
    background: #dbeafe;
    color: #1d4ed8;
}

.performanceWarning {
    background: #fef3c7;
    color: #b45309;
}

.performanceCritical {
    background: #fee2e2;
    color: #dc2626;
}

.analyticsSnapshotItem {
    display: flex;
    justify-content: space-between;
    gap: 12px;
    padding: 14px 0;
    border-bottom: 1px solid #eef2f7;
}

.analyticsSnapshotItem:last-child {
    border-bottom: none;
}

.analyticsSnapshotLabel {
    color: #64748b;
}

.analyticsSnapshotValue {
    color: #0f172a;
    font-weight: 800;
    text-align: right;
}

@media (max-width: 1150px) {
    .analyticsTopCards {
        grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .analyticsGrid {
        grid-template-columns: 1fr;
    }
}

@media (max-width: 650px) {
    .analyticsTopCards {
        grid-template-columns: 1fr;
    }

    .analyticsCategoryTop,
    .analyticsCategoryBottom,
    .analyticsPerformanceItem,
    .analyticsSnapshotItem {
        flex-direction: column;
        align-items: flex-start;
    }

    .analyticsPerformanceRight {
        min-width: 0;
    }
}

.settingsTopGrid,
.settingsBottomGrid {
    display: grid;
    grid-template-columns: 1fr 1.15fr;
    gap: 18px;
    margin-bottom: 18px;
}

.profileSummaryHeader {
    display: flex;
    align-items: center;
    gap: 16px;
    margin-bottom: 24px;
}

.profileSummaryAvatar {
    width: 74px;
    height: 74px;
    border-radius: 50%;
    background: #dbeafe;
    color: #1d4ed8;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
    font-weight: 800;
}

.profileSummaryHeader h2 {
    margin: 0 0 6px;
    color: #0f172a;
    font-size: 1.3rem;
}

.profileSummaryHeader p {
    margin: 0;
    color: #64748b;
}

.profileSummaryStats {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 14px;
}

.profileSummaryStat {
    border: 1px solid #e2e8f0;
    border-radius: 14px;
    padding: 16px;
    background: #f8fbff;
}

.profileSummaryLabel {
    display: block;
    color: #64748b;
    font-size: 0.88rem;
    margin-bottom: 8px;
}

.profileSummaryStat strong {
    color: #0f172a;
    font-size: 1rem;
}

.settingsForm {
    display: flex;
    flex-direction: column;
    gap: 16px;
}

.settingsGrid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 14px;
}

.settingsActionRow {
    display: flex;
    justify-content: flex-start;
    margin-top: 4px;
}

.settingsSuccessMsg {
    margin: 0;
    color: #16a34a;
    font-weight: 600;
    min-height: 20px;
}

.toggleSettingRow {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
    padding: 14px 0;
    border-bottom: 1px solid #eef2f7;
}

.toggleSettingRow:last-of-type {
    border-bottom: none;
}

.toggleSettingRow h3 {
    margin: 0 0 6px;
    font-size: 1rem;
    color: #0f172a;
}

.toggleSettingRow p {
    margin: 0;
    color: #64748b;
    line-height: 1.5;
}

.switch {
    position: relative;
    display: inline-block;
    width: 56px;
    min-width: 56px;
    height: 30px;
}

.switch input {
    opacity: 0;
    width: 0;
    height: 0;
}

.slider {
    position: absolute;
    inset: 0;
    cursor: pointer;
    background: #cbd5e1;
    border-radius: 999px;
    transition: 0.25s ease;
}

.slider::before {
    content: "";
    position: absolute;
    width: 22px;
    height: 22px;
    left: 4px;
    top: 4px;
    background: white;
    border-radius: 50%;
    transition: 0.25s ease;
}

.switch input:checked + .slider {
    background: #2563eb;
}

.switch input:checked + .slider::before {
    transform: translateX(26px);
}

@media (max-width: 1100px) {
    .settingsTopGrid,
    .settingsBottomGrid {
        grid-template-columns: 1fr;
    }
}

@media (max-width: 700px) {
    .settingsGrid,
    .profileSummaryStats {
        grid-template-columns: 1fr;
    }

    .toggleSettingRow {
        flex-direction: column;
        align-items: flex-start;
    }
}

.transactionsTopGrid {
    display: grid;
    grid-template-columns: 1.2fr 0.8fr;
    gap: 18px;
    margin-bottom: 18px;
}

#transactionNotes {
    width: 100%;
    padding: 12px 14px;
    border: 1px solid #dbeafe;
    border-radius: 12px;
    outline: none;
    background: #fff;
    box-sizing: border-box;
    resize: vertical;
    font-family: inherit;
}

.transactionsSummaryList {
    display: flex;
    flex-direction: column;
    gap: 6px;
}

.transactionsToolbar {
    display: flex;
    gap: 12px;
    align-items: center;
    margin-bottom: 18px;
    flex-wrap: wrap;
}

.transactionsFilter {
    min-width: 180px;
    padding: 12px 14px;
    border-radius: 12px;
    border: 1px solid #dbeafe;
    outline: none;
    background: #ffffff;
}

.transactionsList {
    display: flex;
    flex-direction: column;
    gap: 14px;
}

.transactionCardItem {
    display: flex;
    justify-content: space-between;
    gap: 18px;
    padding: 18px;
    border: 1px solid #e2e8f0;
    border-radius: 16px;
    background: #ffffff;
}

.transactionCardLeft {
    display: flex;
    gap: 14px;
    flex: 1;
}

.transactionTypeIcon {
    width: 48px;
    height: 48px;
    border-radius: 14px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.3rem;
    font-weight: 800;
}

.incomeIcon {
    background: #dcfce7;
    color: #15803d;
}

.expenseIcon {
    background: #fee2e2;
    color: #dc2626;
}

.transactionCardContent h3 {
    margin: 0 0 8px;
    font-size: 1rem;
    color: #0f172a;
}

.transactionCardContent p {
    margin: 0 0 6px;
    color: #64748b;
    line-height: 1.5;
}

.transactionCardContent small {
    color: #94a3b8;
    display: block;
    line-height: 1.5;
}

.transactionCardRight {
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    gap: 10px;
    min-width: 120px;
}

@media (max-width: 1100px) {
    .transactionsTopGrid {
        grid-template-columns: 1fr;
    }
}

@media (max-width: 760px) {
    .transactionCardItem {
        flex-direction: column;
    }

    .transactionCardRight {
        align-items: flex-start;
        min-width: 0;
    }
}

@media (max-width: 560px) {
    .transactionsToolbar {
        flex-direction: column;
        align-items: stretch;
    }

    .transactionsFilter {
        width: 100%;
        min-width: 0;
    }

    .transactionCardLeft {
        flex-direction: column;
    }
}

/* ===== Login Page ===== */
.loginPage {
    margin: 0;
    min-height: 100vh;
    font-family: Arial, sans-serif;
    background:
        radial-gradient(circle at top left, rgba(37, 99, 235, 0.18), transparent 30%),
        radial-gradient(circle at bottom right, rgba(59, 130, 246, 0.15), transparent 30%),
        linear-gradient(135deg, #eff6ff, #dbeafe, #f8fafc);
    color: #0f172a;
}

/* ===== Top Bar ===== */
.topbar {
    width: 100%;
    padding: 20px 40px;
    box-sizing: border-box;
}

.logo {
    margin: 0;
    font-size: 1.5rem;
    font-weight: 700;
    color: #1d4ed8;
    letter-spacing: 0.3px;
}

/* ===== Centering ===== */
.loginContainer {
    min-height: calc(100vh - 80px);
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 30px 20px 50px;
    box-sizing: border-box;
}

/* ===== Card ===== */
.loginCard {
    width: 100%;
    max-width: 440px;
    background: rgba(255, 255, 255, 0.88);
    backdrop-filter: blur(14px);
    border: 1px solid rgba(255, 255, 255, 0.65);
    border-radius: 24px;
    box-shadow: 0 20px 60px rgba(15, 23, 42, 0.12);
    padding: 38px 32px;
    box-sizing: border-box;
}

.loginCard h1 {
    margin: 0 0 10px;
    font-size: 2rem;
    color: #0f172a;
}

.subtitle {
    margin: 0 0 28px;
    font-size: 0.98rem;
    line-height: 1.6;
    color: #64748b;
}

/* ===== Form ===== */
.loginForm {
    display: flex;
    flex-direction: column;
    gap: 18px;
}

.inputGroup {
    display: flex;
    flex-direction: column;
    gap: 8px;
}

.inputGroup label {
    font-size: 0.95rem;
    font-weight: 600;
    color: #1e293b;
}

.inputGroup input {
    width: 100%;
    box-sizing: border-box;
    padding: 14px 16px;
    border: 1px solid #cbd5e1;
    border-radius: 14px;
    background: #ffffff;
    font-size: 0.96rem;
    color: #0f172a;
    outline: none;
    transition: 0.25s ease;
}

.inputGroup input::placeholder {
    color: #94a3b8;
}

.inputGroup input:focus {
    border-color: #2563eb;
    box-shadow: 0 0 0 4px rgba(37, 99, 235, 0.14);
}

/* ===== Options Row ===== */
.formOptions {
    margin-top: -2px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    flex-wrap: wrap;
}

.rememberMe {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 0.92rem;
    color: #475569;
    cursor: pointer;
}

.rememberMe input {
    accent-color: #2563eb;
}

.forgotPassword {
    font-size: 0.92rem;
    font-weight: 600;
    text-decoration: none;
    color: #2563eb;
    transition: 0.2s ease;
}

.forgotPassword:hover {
    color: #1d4ed8;
    text-decoration: underline;
}

/* ===== Button ===== */
.loginMainBtn {
    margin-top: 8px;
    border: none;
    border-radius: 14px;
    padding: 15px 18px;
    background: linear-gradient(135deg, #2563eb, #1d4ed8);
    color: white;
    font-size: 1rem;
    font-weight: 700;
    cursor: pointer;
    box-shadow: 0 12px 24px rgba(37, 99, 235, 0.22);
    transition: 0.25s ease;
}

.loginMainBtn:hover {
    transform: translateY(-2px);
    box-shadow: 0 16px 28px rgba(37, 99, 235, 0.28);
}

.loginMainBtn:active {
    transform: translateY(0);
}

/* ===== Bottom Text ===== */
.signupText {
    margin: 22px 0 0;
    text-align: center;
    font-size: 0.95rem;
    color: #64748b;
}

.signupText a {
    color: #2563eb;
    text-decoration: none;
    font-weight: 700;
}

.signupText a:hover {
    text-decoration: underline;
}

/* ===== Error Message ===== */
.errorMsg {
    margin: -4px 0 0;
    font-size: 0.9rem;
    color: #dc2626;
    min-height: 18px;
}

/* ===== Responsive ===== */
@media (max-width: 520px) {
    .topbar {
        padding: 18px 20px;
    }

    .loginCard {
        padding: 28px 20px;
        border-radius: 20px;
    }

    .loginCard h1 {
        font-size: 1.7rem;
    }

    .formOptions {
        flex-direction: column;
        align-items: flex-start;
    }

    .adminLoginBtn {
  width: 100%;
  margin-top: 12px;
  padding: 14px;
  border-radius: 25px;
  border: none;
  background: transparent;
  color: #3b5bdb;
  font-size: 15px;
  font-weight: 600;
  cursor: pointer;
  text-align: center;
}

.adminLoginBtn:hover {
  text-decoration: underline;
}
}

/* ===== Onboarding Page Fix ===== */

.welcomePageBody {
  min-height: 100vh;
  background: linear-gradient(135deg, #eef5ff, #ffffff);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 30px;
}

.welcomePage {
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
}

.welcomeCard {
  width: 100%;
  max-width: 540px;
  background: white;
  border-radius: 28px;
  padding: 44px 40px;
  text-align: center;
  box-shadow: 0 22px 55px rgba(37, 99, 235, 0.14);
  animation: welcomeFadeUp 0.8s ease both;
}

.onboardingLogo {
  width: 260px;
  height: 180px;   
  object-fit: cover;
  object-position: center;

  display: block;
  margin: 0 auto 24px;
  animation: logoFloat 3s ease-in-out infinite;
  
}

.welcomeTitle {
  font-size: 34px;
  font-weight: 800;
  color: #0f2748;
  margin-bottom: 14px;
}

.welcomeTitle span {
  color: #2563eb;
}

.welcomeSubtitle {
  max-width: 430px;
  margin: 0 auto 30px;
  color: #64748b;
  font-size: 16px;
  line-height: 1.7;
}

.welcomeActions {
  display: flex;
  flex-direction: column;
  gap: 14px;
}

.welcomeActions .fullBtn {
  width: 100%;
  height: 52px;
  border-radius: 14px;
  font-weight: 700;
}

@keyframes welcomeFadeUp {
  from {
    opacity: 0;
    transform: translateY(35px) scale(0.96);
  }

  to {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

@keyframes logoFloat {
  0%, 100% {
    transform: translateY(0) scale(1);
  }
  50% {
    transform: translateY(-10px) scale(1.03);
  }
}

.sidebarLogo {
  display: flex;
  align-items: center;
  gap: 10px;
}

.sidebarLogoImg {
  width: 80px;
  height: 80px;
  object-fit: contain;
}

.logo {
  display: flex;
  align-items: center;
  gap: 10px;
}

.brandLogoImg {
  width: 90px;   /* try 48, 56, or 64 */
  height: 90px;
  object-fit: contain;
}